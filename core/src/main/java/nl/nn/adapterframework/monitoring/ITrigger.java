package nl.nn.adapterframework.monitoring;

/**
 * Needed for the Frank!Doc. Only config children referencing an interface can
 * reference the same Java class under different XML element names.
 * @author martijn
 *
 */
public interface ITrigger {
}
