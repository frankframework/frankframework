<Configuration>
	<!-- Run Larva scenario's (on startup and once a day scheduled) -->
	<Include ref="ConfigurationLarva.xml"/>
	<Include ref="ConfigurationTestSendMail.xml"/>

	<!-- Include configuration for console functions -->
	<Include ref="ConfigurationUploadFiles.xml"/>

	<!-- Run saved Ladybug test reports at a configurable CRON interval -->
	<Include ref="ConfigurationLadybug.xml"/>

	<Include ref="ConfigurationXmlParsers.xml"/>
</Configuration>