scenario.description = Test Parameterized ErrorFormatter Success (via Exception) (Call Direct)

include = common.properties

step1.java.ParameterizedErrorFormatter-CallDirect.write = scenario07/test-input.json

# In case of success, the pipeline just echoes the input so we can read that back
step2.java.ParameterizedErrorFormatter-CallDirect.read = scenario07/test-input.json
