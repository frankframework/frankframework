scenario.description = Test Parameterized ErrorFormatter Success (via Exception)

include = common.properties

step1.java.ParameterizedErrorFormatter.write = scenario07/test-input.json

# In case of success, the pipeline just echoes the input so we can read that back
step2.java.ParameterizedErrorFormatter.read = scenario07/test-input.json
