<project default="copy.files">
	<target name="copy.files">
		<property name="root.dir" value="${testdata.dir}/zip" />
		<delete dir="${root.dir}" />
		<mkdir dir="${root.dir}" />
	</target>
</project>
