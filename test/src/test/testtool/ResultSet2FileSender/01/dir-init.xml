<project default="copy.files">
	<target name="copy.files">
		<property name="root.dir" value="${testdata.dir}/rs2fs" />
		<delete dir="${root.dir}" />
		<mkdir dir="${root.dir}" />
	</target>
</project>
