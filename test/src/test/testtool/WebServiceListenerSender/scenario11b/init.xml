<project default="copy.files">
	<target name="copy.files">
		<property name="root.dir" value="${testdata.dir}/webservices" />
		<delete dir="${root.dir}" />
		<mkdir dir="${root.dir}" />
		<copy file="textfile.txt" todir="${root.dir}" />
	</target>
</project>