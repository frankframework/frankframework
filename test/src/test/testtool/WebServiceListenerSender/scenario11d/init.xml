<project default="copy.files">
	<target name="copy.files">
		<property name="root.dir" value="${testdata.dir}/webservices-11d" />
		<delete dir="${root.dir}" />
		<mkdir dir="${root.dir}" />
		<copy file="filetext.txt" todir="${root.dir}" />
	</target>
</project>
