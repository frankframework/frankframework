<project default="copy.file">
	<target name="copy.file">
		<property name="root.dir" value="${testdata.dir}/FileSender" />
		<delete dir="${root.dir}" />
		<mkdir dir="${root.dir}" />
		<copy file="toCopy.txt" todir="${root.dir}" />
	</target>
</project>