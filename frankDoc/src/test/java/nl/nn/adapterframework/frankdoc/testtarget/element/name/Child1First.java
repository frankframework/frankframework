package nl.nn.adapterframework.frankdoc.testtarget.element.name;

// Should become Child1B because it implements IFirst. That interface is a
// common interface end hence the word "First" is removed.
public class Child1First implements IThird {

}
