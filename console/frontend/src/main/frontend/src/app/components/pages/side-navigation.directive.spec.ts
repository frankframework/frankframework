import { SideNavigationDirective } from './side-navigation.directive';

describe('SideNavigationDirective', () => {
  it('should create an instance', () => {
    const directive = new SideNavigationDirective();
    expect(directive).toBeTruthy();
  });
});
