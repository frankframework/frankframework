import { HasAccessToLinkDirective } from './has-access-to-link.directive';

describe('HasAccessToLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new HasAccessToLinkDirective();
    expect(directive).toBeTruthy();
  });
});
