import { ToDateDirective } from './to-date.directive';

describe('ToDateDirective', () => {
  it('should create an instance', () => {
    const directive = new ToDateDirective();
    expect(directive).toBeTruthy();
  });
});
