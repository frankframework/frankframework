import { DtContentDirective } from './dt-content.directive';

describe('DtContentDirective', () => {
  it('should create an instance', () => {
    const directive = new DtContentDirective();
    expect(directive).toBeTruthy();
  });
});
