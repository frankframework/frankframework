import { TimeSinceDirective } from './time-since.directive';

describe('TimeSinceDirective', () => {
  it('should create an instance', () => {
    const directive = new TimeSinceDirective();
    expect(directive).toBeTruthy();
  });
});
