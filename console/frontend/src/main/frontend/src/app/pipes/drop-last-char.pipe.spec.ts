import { DropLastCharPipe } from './drop-last-char.pipe';

describe('DropLastCharPipe', () => {
  it('create an instance', () => {
    const pipe = new DropLastCharPipe();
    expect(pipe).toBeTruthy();
  });
});
