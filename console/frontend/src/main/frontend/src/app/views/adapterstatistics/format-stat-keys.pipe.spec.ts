import { FormatStatKeysPipe } from './format-stat-keys.pipe';

describe('FormatStatKeysPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatStatKeysPipe();
    expect(pipe).toBeTruthy();
  });
});
