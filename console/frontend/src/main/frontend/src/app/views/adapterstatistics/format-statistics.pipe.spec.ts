import { FormatStatisticsPipe } from './format-statistics.pipe';

describe('FormatStatisticsPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatStatisticsPipe();
    expect(pipe).toBeTruthy();
  });
});
