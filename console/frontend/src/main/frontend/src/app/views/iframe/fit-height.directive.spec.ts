import { FitHeightDirective } from './fit-height.directive';

describe('FitHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new FitHeightDirective();
    expect(directive).toBeTruthy();
  });
});
