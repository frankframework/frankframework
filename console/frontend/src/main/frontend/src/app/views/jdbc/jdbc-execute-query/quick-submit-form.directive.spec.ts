import { QuickSubmitFormDirective } from './quick-submit-form.directive';

describe('QuickSubmitFormDirective', () => {
  it('should create an instance', () => {
    const directive = new QuickSubmitFormDirective();
    expect(directive).toBeTruthy();
  });
});
