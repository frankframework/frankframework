name: Initialize SonarCloud Checks

on:
  pull_request:
    branches:
      - master
      - '[0-9]+.[0-9]+-release'
      - 'release/[0-9]+.[0-9]+'

jobs:
  sonarcloud:
    name: SonarCloud
    runs-on: ubuntu-latest
    steps:
      - name: Start green
        run: echo "Start green untill real results are in"

  sonarcloud-code-analysis:
    name: SonarCloud Code Analysis
    runs-on: ubuntu-latest
    steps:
      - name: Start green
        run: echo "Start green untill real results are in"
