name: Mandatory Check Always Successful

on:
  pull_request:
    branches:
      - '[0-9]+.[0-9]+-release'
      - 'release/[0-9]+.[0-9]+'

jobs:
  sonarcloud:
    name: Build and Test Maven Artifacts on JDK
    runs-on: ubuntu-latest
    steps:
      - name: Yes green! ✅
        run: echo "Because we've got a matrix test the name is prepended in the main workflow. Hence we use this workflow to allow mergin instead!"