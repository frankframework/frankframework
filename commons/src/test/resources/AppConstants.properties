# Keep this small-ish for testing so we don't need ridiculous sizes of data to make a test that exceeds this value
# This is 128KiB, 2 times the default buffer size (64KiB) in StreamUtil
message.max.memory.size=131072

# These test-properties for test org.frankframework.util.AppConstantsTest.testSubAppConstants must be defined in the base AppConstants.properties files
test.basekey.prop1=1
test.basekey.prop2=2
test.basekey.prop3=3
