web.protocol=http
web.port=8080
web.contextpath=/iaf-test

authAliases.expansion.allowed=testalias
configurations.allowCustomClasses=true

# The LargeBlockTester class used by this test doesn't work. We are disabling it instead of a backport.
# It will still be tested in other appservers regularly.
active.streaming-large-messages=false
