version: ''

services:
  ff-test:
    environment:
      jdbc.dbms.default: mariadb
      jdbc.hostname: mariadb
      active.storedProcedureTests: true

  mariadb:
    image: ghcr.io/frankframework/dbms/mariadb
    ports:
      - "3306:3306"
