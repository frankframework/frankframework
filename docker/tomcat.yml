version: ''

name: tomcat

services:
  ff-test:
    volumes:
      - "../test/src/test/testtool:/opt/frank/testtool-ext"
    build: Tomcat/test
    ports:
      - "8080:8080"
    environment:
      larva.adapter.active: false
